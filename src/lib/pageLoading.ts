/** Full-height loading area inside dashboard shell (sidebar + header). */
export const PAGE_LOADING_MIN_HEIGHT = 'min-h-[calc(100dvh-4rem)]';

/** Loading block inside a dashboard page section (below header). */
export const DASHBOARD_SECTION_LOADING_MIN_HEIGHT = 'min-h-[calc(100dvh-8rem)]';
