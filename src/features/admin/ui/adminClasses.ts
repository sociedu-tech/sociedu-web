/** Lớp dùng chung trang quản trị — chỉ slate + primary (đồng bộ sidebar / dashboard). */
export const adminSelect =
  'rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm text-slate-800 outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/20';

export const adminBtnPrimary =
  'inline-flex items-center justify-center gap-1 rounded-lg bg-primary px-2.5 py-1.5 text-xs font-semibold text-white transition hover:opacity-95';

export const adminBtnGhost =
  'inline-flex items-center justify-center gap-1 rounded-lg border border-slate-200 bg-white px-2.5 py-1.5 text-xs font-medium text-slate-700 transition hover:bg-slate-50';
