export default function AdminMentorPackagesPage({ params }: { params: { mentorId: string } }) {
  return (
    <div>
      {/* TODO: Hiển thị các gói dịch vụ của Mentor {params.mentorId} + Nút duyệt/khóa */}
    </div>
  );
}
