# Public API (browser — must be NEXT_PUBLIC_*)
NEXT_PUBLIC_API_BASE_URL=http://localhost:9992

# Realtime: NEXT_PUBLIC_API_BASE_URL + /api/v1/ws (SockJS/STOMP, cùng host/port với REST).

# Production example:
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
